<h1>All tasks:</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %> - (<%= link_to 'Delete', destroy_task_path(task), method: :delete %>)</li>
  <% end %>
</ul>

<%= link_to 'Create a new task', new_task_path %>
